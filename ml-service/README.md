# BiasLens AI ML Service

FastAPI placeholder service for future model and bias inference pipelines.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```
